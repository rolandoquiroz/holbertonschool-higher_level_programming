# 1-my_list.txt

The ``1-my_list`` module
========================

Using ``MyList``
----------------

Test the module by importing the class and running it through a series of
interactive tests.

    Test 00 Import class MyList from 1-my_list module
    >>> MyList = __import__('1-my_list').MyList

Now test:

    Test 01 - Instantiation, inherits from list, `__str__`
    >>> my_list = MyList()
    >>> print(my_list)
    []

    Test 02 - `append()`
    >>> my_list.append(666)
    >>> print(my_list)
    [666]
